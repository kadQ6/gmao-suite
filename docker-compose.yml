services:
  db:
    image: postgres:16-alpine
    container_name: gmao-suite-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: gmao_suite
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
